{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{# Generic CRUD workbench template.

   This page stays shell-first by default. The interactive provider mount is an
   explicit opt-in via ?interactive=1 so ordinary visible routes stay fast.
#}
{% extends 'interfacing/admin/body/provider_page.html.twig' %}

{% set adminProviderPageTitle = adminProviderPageTitle|default('Crud Explorer') %}
{% set adminProviderResourceName = adminProviderResourceName|default('interfacing_page_crud_explorer') %}
{% set adminProviderResourceLabel = adminProviderResourceLabel|default(adminProviderPageTitle) %}
{% set adminProviderOperation = adminProviderOperation|default('index') %}
{% set adminProviderSurface = adminProviderSurface|default('admin') %}
{% set screenId = screenId|default('crud.generic') %}
