# Interfacing service contracts

This directory contains service contracts for the Interfacing component. New contracts should use capability-specific folders when the vocabulary is narrower than the whole component, for example `Crud/`, `View/`, `Catalog/`, `Shell/`, `Access/`, `Runtime/`, or `Provider/`. Root-level interfaces are transitional compatibility aliases unless a manifest says otherwise.
