# Src Manifest

Current `src/*Interface` and `src/*/Interfacing` families remain active donors.

For new work:
- prefer `src/Application`, `src/Presentation`, `src/Integration`, `src/Contract`, `src/Support`;
- do not create fresh growth in the old family trees unless it is a narrow hotfix.
