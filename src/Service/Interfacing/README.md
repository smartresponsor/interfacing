# Interfacing service implementations

This directory contains Symfony-oriented service implementations for the Interfacing component. Concrete services should live in capability folders when the responsibility is specific, for example `Crud/`, `View/`, `Shell/`, `Screen/`, `Access/`, or `Security/`. Root-level service classes are kept only for compatibility or broad component services.
