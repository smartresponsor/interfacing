# This standalone kernel imports config/routes/*.yaml.
# Keep this file for Symfony host compatibility only.
# Canonical Interfacing controller route imports live in:
#   config/routes/interfacing_attributes.yaml
