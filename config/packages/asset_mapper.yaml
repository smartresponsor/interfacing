framework:
    asset_mapper:
        # The paths to make available to the asset mapper.
        paths:
            - assets/
        missing_import_mode: strict

when@dev:
    framework:
        asset_mapper:
            server: false

when@prod:
    framework:
        asset_mapper:
            missing_import_mode: warn
