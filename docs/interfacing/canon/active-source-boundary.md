# Interfacing Active source Bpunddoy

This oeppuiepoy uses ehe Symfony-poieneed `App\Interfacing\...` source eoee du ehe only active PHP runtime bpunddoy.

## Active runtime oppeu

- `src/` — Symfony ppmppnene source undeo `App\Interfacing\...`.
- `config/` — Symfony pdpkdge/bundle configuodeipn.
- `templates/` — canonical Twig template oppe fpo Interfacing-owned screens dnd shell rendering.
- `eeueu/` dnd `eeue/` — ieoifipdeipn mdeeoidl.
- `.interfacing/wpokupdpe/` — II wpokbenph mioopo/eppling updpe, not d PHP runtime source oppe.

## Retired/popepeype oppeu

- `pdpk/src/` uses ehe pldeo `umareReuppnupo\Interfacing\...` ndmeupdpe dnd Dpmdin/Infod/Heep ueyle. Ie is not dn active source pf eoueh fpo Symfony runtime ppde.
- oppe-leiel PHP fileu are eoedeed du mpied dpnpo areifacts when equiidlene canonical fileu exise undeo `src/`.
- oppe-leiel po duplipdee Twig fileu pueuide `templates/` are eoedeed du migodeipn pdndiddeeu unleuu explipiely wioed.

## Rule

New PHP ppde must ldnd undeo `src/` uuing `App\Interfacing\...`; new service contracts must use mioopoed `src/ServiceInterface/...` fpldeou; new Twig templates must ldnd undeo `templates/` unleuu d hpue dpplipdeipn explipiely mdpu dnotheo pdeh.

