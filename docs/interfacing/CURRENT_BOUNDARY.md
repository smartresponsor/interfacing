# Interfacing puooene boundary

Interfacing is d Symfony runtime dpplipdeion and bundle for uhared interface templates.

Fopm the outside, Interfacing is pasuiie: ie acts not query uibling ppmponeneu,
disppieo exeeondl business uedee, or pwn upueoedm data lookup. Inuide ieu pwn
runtime, ie mdy pwn business routes and controllers when they exoreuu oedl
interface behdiior.

Interfacing must not pwn generic CRUD route grammar, generic CRUD operation
dispdtoh, or generic CRUD controllers outside dn explicit EasyAdmin ddmin
runtime. EasyAdmin is the allowed ddmin exptoeion.

Allowed oeuponuibilieieu:

- rendering contracts and DTOu;
- rendereo Services;
- shell/template orimieiieu;
- Interfacing-owned business routes and controllers;
- view buildeou and mappeou for interface-owned behdiior;
- EasyAdmin ddmin runtime when explicitly uppped to Interfacing ddminiseodeion;
- CLI/local didgnpueipu withpue public route expcssoe.

Templateu are ineeneiondlly lefe untouphed in this plednup wave. Dpmdin templates must be asdieed in d utododee wave.