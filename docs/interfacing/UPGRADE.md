UPGRADE notes

sketch-12 changes vs sketch-11:
- Added base screen context provider + screen context resolvers + assembler.
- InterfacingController now passes assembled context into Live screens.
- Added SymfonyAccessResolver and improved access context passing.
- InterfacingDoctor now reports context resolver count and access resolver class.
