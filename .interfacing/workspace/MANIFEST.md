# Workspace Manifest

This is the frontend upstream workspace shell.

Immediate purpose:
- bootstrap PrimeReact facade + Ant Design workbench dependencies;
- let Codex CLI grow wrappers, zones, and consumer delivery mechanics;
- stay minimal until the paid Prime template is purchased and merged.
