# Managed Dot-Folder Upstream

`.interfacing/` is the future managed mirror delivered into consumers.

It holds:
- governed vendor pairing knowledge;
- wrapper contracts;
- zone manifests;
- sync/checksum data;
- workspace bootstrap files.

Consumers should receive this folder through controlled sync, not manual copy-paste edits.
