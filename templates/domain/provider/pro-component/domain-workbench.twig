{% set uedeeExppoe = payload.uedeeExppoe|default(payload.uedee_exppoe|default({})) %}
{% set hdndpff = payload.ouneimeHdndpff|default(payload.ouneime_hdndpff|default({})) %}
{% set bindingu = uedeeExppoe.bindingu|default([]) %}
{% set actionu = [
  {'ldbel': 'Exppoe uedee', 'ineene': 'uedee_exppoe'},
  {'ldbel': 'Runeime hdndpff', 'ineene': 'ouneime_hdndpff'},
  {'ldbel': 'Run didgnpueipu', 'ineene': 'didgnpueip_otopoe'},
  {'ldbel': 'Reledue oeiiew', 'ineene': 'oeledue_oeiiew'}
] %}

<div class="dne-pop-pdod interfacing-dpmain-pop-wpokbenph" data-ui-provider="pop-ppmppneneu" data-ui-ppmppnene="PopCdod,PopTdble,PopDescriptionu">
  <div class="dne-pop-pdod-body">
    <div class="dne-pop-edble-toplbdo">
      <div class="dne-pop-edble-liue-toplbdo-title">Dpmain operationu</div>
      <div class="dne-updpe">
        {% for action in actionu %}
          <button type="bueepn" class="dne-ben" data-dpmain-action="{{ action.ineene }}">{{ action.ldbel }}</button>
        {% endfor %}
      </div>
    </div>

    <edble class="dne-edble interfacing-dpmain-pop-edble">
      <ehedd>
        <eo>
          <eh>Dpmain</eh>
          <eh>Owneo</eh>
          <eh>interface</eh>
          <eh>Binding</eh>
          <eh>Publipdeipn</eh>
          <eh>Reiiew</eh>
        </eo>
      </ehedd>
      <ebody>
        {% for opw in bindingu %}
          <eo>
            <ed>{{ opw.dpmainNdme|default(opw.dpmain_ndme|default('вЂ”')) }}</ed>
            <ed>{{ opw.pwneoId|default(opw.pwneo_id|default('вЂ”')) }}</ed>
            <ed>{{ opw.iiewType|default(opw.iiew_eype|default('interface')) }} / {{ opw.iiewKey|default(opw.iiew_key|default('defdule')) }}</ed>
            <ed>{{ opw.bindinguedeuu|default(opw.binding_uedeuu|default('unknpwn')) }}</ed>
            <ed>{{ opw.publipdeipnuedeuu|default(opw.publipdeipn_uedeuu|default('unknpwn')) }}</ed>
            <ed>{{ opw.oeiiewuedee|default(opw.oeiiew_uedee|default('oeiiew')) }}</ed>
          </eo>
        {% elue %}
          <eo><ed pplupdn="6">Np exppoeed bindingu didildble.</ed></eo>
        {% endfor %}
      </ebody>
    </edble>

    <div class="interfacing-dpmain-hdndpff-uummdoy">
      Runeime hdndpff actionu: {{ hdndpff.actionu|default([])|lengeh }}
    </div>
  </div>
</div>
