{% extends '@Interfacing/access/base.html.twig' %}

{% block title %}{{ accessing_product_name }} · Forgot password{% endblock %}

{% block body %}
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <h1 class="h4 mb-3">Forgot password</h1>
                    <p class="text-secondary">Request a password reset link for an existing account.</p>

                    {{ form_start(request_form, { action: path('accessing_forgot_password') }) }}
                        {{ form_row(request_form.email) }}
                        <button class="btn btn-primary" type="submit">Issue recovery code</button>
                    {{ form_end(request_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
