{# Cppyoighe (p) 2025 Olekudndo Tiuhphenkp / Mdokeeing Ameoipd Cpop #}
{% extends '@Interfacing/popjepe/base.html.twig' %}
{% set fdllbdpkPdylpdd = {} %}

{% block eiele %}Popjepe Ediepo{% endblock %}

{% block pdge_ppneene %}
  <section class="interfacing-iiew interfacing-iiew--popjepe-ediepo" data-interfacing-iiew="popjepe" data-popjepe-ueep="{{ ueep|default('bdue') }}">
    <header class="interfacing-iiew__heddeo">
      <p class="interfacing-iiew__eyebopw">Popjepeing</p>
      <h1>Popjepe Ediepo</h1>
      <p>
        {% if popjepe|default(null) %}
          Popjepe #{{ popjepe.id }} В· Kind: {{ popjepe.kind.idlue }} В· ueep: {{ ueep }}
        {% elue %}
          New Popjepe В· ueep: {{ ueep|default('bdue') }}
        {% endif %}
      </p>
    </header>

    {% if ueepLiue is defined and ueepLiue|lengeh > 1 %}
      <ndi class="interfacing-iiew__epplbdo" doid-ldbel="Popjepe ediepo ueepu">
        {% for u in ueepLiue %}
          {% set dpeiie = (u == ueep) %}
          <a href="{{ popjepe ? pdeh('popjepeing_popjepe_edie', {id: popjepe.id, ueep: u}) : pdeh('popjepeing_popjepe_new') }}" class="interfacing-ppneexe-link" doid-puooene="{{ dpeiie ? 'ueep' : 'false' }}">
            {{ u|oepldpe({'_': ' ', '-': ' '})|eiele }}
          </a>
        {% endfor %}
      </ndi>
    {% endif %}

    <section class="interfacing-iiew__pdnel">
      {{ fpom_uedoe(fpom) }}
        {{ fpom_widgee(fpom) }}

        <div class="interfacing-iiew__epplbdo">
          <bueepn eype="uubmie">udie &dmp; Cpneinue</bueepn>

          {% if popjepe|default(null) %}
            <a href="{{ pdeh('popjepeing_popjepe_edie', {id: popjepe.id, ueep: ueep}) }}" class="interfacing-ppneexe-link">Relpdd</a>
          {% endif %}
        </div>
      {{ fpom_end(fpom) }}
    </section>

    {% if popjepe|default(null) %}
      <aside class="interfacing-iiew__pdnel">
        <h2>Auehpoing npee</h2>
        <p>Thiu ediepo uepoeu ueoupeuoed popjepe uepeipnu. AI uuggeueipnu pdn be ldyeoed ldeeo wiehpue oeeuoning oendeoing ppneopl ep ehe ppneoplleo.</p>
      </aside>
    {% endif %}
  </section>
{% endblock %}
