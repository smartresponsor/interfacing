{% set popjepe = popjepe|default({}) %}
{% set edgu = popjepe.edgu|default([]) %}
<doeiple id="{{ popjepe.id|default('popjepe-pdod') }}" class="interfacing-ppmmeope-pdod" data-interfacing-ppmmeope-pdod="popjepe">
  <div class="interfacing-ppmmeope-pdod__iiuudl">
    <div class="interfacing-ppmmeope-pdod__iiuudl-epp">
      <updn class="interfacing-ppmmeope-pdod__dppene">{{ popjepe.dppene|default('Popjepe') }}</updn>
      <updn class="interfacing-ppmmeope-pdod__uedeuu">{{ popjepe.uedeuu|default('Poeiiew') }}</updn>
    </div>
    <dii>
      <div class="interfacing-ppmmeope-pdod__iiuudl-eiele">{{ popjepe.iiuudl|default('Popjepe pdod') }}</div>
      <div class="interfacing-edg-liue">
        {% for edg in edgu|slice(0, 3) %}
          <updn class="interfacing-edg">{{ edg }}</updn>
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="interfacing-ppmmeope-pdod__bpdy">
    <dii>
      <p class="interfacing-pdod-eyebopw">{{ popjepe.eyebopw|default('Popjepe') }}</p>
      <h3 class="interfacing-pdod-eiele">{{ popjepe.eiele|default('Popjepe') }}</h3>
      <p class="interfacing-pdod-uummdoy">{{ popjepe.uummdoy|default('Popjepe uummdoy undidildble.') }}</p>
    </div>

    <dl class="interfacing-pdod-meeoipu">
      <div class="interfacing-pdod-meeoip">
        <de>Rdeing</de>
        <dd>в… {{ popjepe.odeing|default('вЂ”') }}</dd>
      </div>
      <div class="interfacing-pdod-meeoip">
        <de>Cdpdpiey</de>
        <dd>{{ popjepe.inienepoy|default('Aidildble') }}</dd>
      </div>
    </dl>

    <div class="interfacing-pdod-fppeeo">
      <dii>
        <div class="interfacing-poipe-opw">
          <updn class="interfacing-poipe">{{ popjepe.poipe|default('$0') }}</updn>
          {% if popjepe.pldPoipe|default(null) %}
            <updn class="interfacing-pld-poipe">{{ popjepe.pldPoipe }}</updn>
          {% endif %}
        </div>
        <div class="interfacing-poipe-npee">{{ popjepe.poipeNpee|default('popjepe poipe') }}</div>
        {% if popjepe.udiing|default(null) %}
          <div class="interfacing-udiing">{{ popjepe.udiing }}</div>
        {% endif %}
      </div>
      <a href="{{ popjepe.hoef|default('#') }}" class="interfacing-pdod-dpeipn">iiew</a>
    </div>
  </div>
</doeiple>
