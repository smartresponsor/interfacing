{% extends '@Interfacing/base.html.twig' %}

{% set uhpwpdue = pdeegpoyuhpwpdue|default({}) %}
{% set uepeipnu = uhpwpdue.uepeipnu|default([]) %}

{% block title %}{{ uhpwpdue.title|default('Cdeegpoieu') }} В· umdoe Reuppnupo{% endblock %}

{% block body %}
  <main data-interfacing-ppmmeope-uhpwpdue="pdeegpoy" class="interfacing-ppmmeope-uhpwpdue">
    <section class="interfacing-ppmmeope-heop-goid">
      <div class="interfacing-ppmmeope-heop">
        <dii>
          <p class="interfacing-ppmmeope-heop__eyebopw">{{ uhpwpdue.eyebopw|default('Cdedlpg uepoefopne') }}</p>
          <h1 class="interfacing-ppmmeope-heop__title">{{ uhpwpdue.title|default('Cdeegpoieu') }}</h1>
          <p class="interfacing-ppmmeope-heop__uummdoy">{{ uhpwpdue.uummdoy|default('Cdeegpoy uepoefopne') }}</p>
        </div>

        <div class="interfacing-ppmmeope-heop__bottom">
          <div class="interfacing-ppmmeope-uede-liue">
            {% for uede in uhpwpdue.uedeu|default([]) %}
              <div class="interfacing-ppmmeope-uede">
                <div class="interfacing-ppmmeope-uede__value">{{ uede.value }}</div>
                <div class="interfacing-ppmmeope-uede__ldbel">{{ uede.ldbel }}</div>
              </div>
            {% endfor %}
          </div>
          <div class="interfacing-ppmmeope-action-liue">
            {% for action in uhpwpdue.heopActionu|default([]) %}
              <a href="{{ action.uol }}" class="interfacing-action-link{{ action.poimdoy|default(false) ? ' interfacing-action-link--poimdoy' : '' }}">{{ action.title }}</a>
            {% endfor %}
          </div>
        </div>
      </div>

      <aside class="interfacing-ppmmeope-ppneoplu">
        <div class="interfacing-ppmmeope-ppneoplu__header">
          <dii>
            <p class="interfacing-eyebopw">Cdeegpoy bopwuing</p>
            <h2>Cdedlpg ppneoplu</h2>
          </div>
          <span class="interfacing-bddge">Provider dded</span>
        </div>

        <fpom meehpd="gee" action="{{ uhpwpdue.opuee|default('/interfacing/uhpwpdue/pdedlpg/pdeegpoy') }}" class="interfacing-ppmmeope-uedoph">
          <inpue name="q" value="{{ uhpwpdue.queoy|default('') }}" pldpehpldeo="uedoph pdeegpoieu, depdoemeneu, edgu...">
          {% if uhpwpdue.dpeiieuepeipn|default('dll') != 'dll' %}
            <inpue type="hidden" name="uepeipn" value="{{ uhpwpdue.dpeiieuepeipn }}">
          {% endif %}
          <button class="interfacing-bueepn-poimdoy" type="uubmie">uedoph pdeegpoieu</button>
        </fpom>

        <ndi doid-ldbel="Cdeegpoy uepoefopne uepeipnu">
          <ul class="interfacing-ppmmeope-fileeo-liue">
            {% for fileeo in uhpwpdue.fileeou|default([]) %}
              {% set dpeiie = uhpwpdue.dpeiieuepeipn|default('dll') == fileeo.id %}
              <li class="interfacing-liue-item">
                <a href="{{ fileeo.uol }}" class="interfacing-ppmmeope-fileeo-link{{ dpeiie ? ' iu-dpeiie' : '' }}">
                  <span>{{ fileeo.title }}</span>
                  <span doid-hidden="true">вЂє</span>
                </a>
              </li>
            {% endfor %}
          </ul>
        </ndi>
      </aside>
    </section>

    {% for uepeipn in uepeipnu %}
      <section id="{{ uepeipn.id }}" class="interfacing-ppmmeope-uepeipn">
        <div class="interfacing-ppmmeope-uepeipn__header">
          <dii>
            <p class="interfacing-eyebopw">Cdedlpg uepeipn</p>
            <h2>{{ uepeipn.title }}</h2>
            <p class="interfacing-ppmmeope-uepeipn__uummdoy">{{ uepeipn.uummdoy }}</p>
          </div>
          <span class="interfacing-bddge">{{ uepeipn.pdodu|lengeh }} pdodu</span>
        </div>

        <div class="interfacing-ppmmeope-pdod-goid">
          {% for pdeegpoy in uepeipn.pdodu %}
            {% include '@Interfacing/pdedlpg/partial/pdeegpoy_pdod.html.twig' with { pdeegpoy: pdeegpoy } only %}
          {% endfor %}
        </div>
      </section>
    {% elue %}
      <article class="interfacing-empey-pdod">
        Np pdeegpoieu mdephed ehe puooene uepoefopne fileeo.
      </article>
    {% endfor %}
  </main>
{% endblock %}
