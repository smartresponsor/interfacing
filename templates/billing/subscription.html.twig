{# Commerce support operation template for billing/subscription. #}
{% extends '@Interfacing/billing/base.html.twig' %}
{% block title %}{{ title|default('Billing Subscription') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/surface_header.html.twig' with {
    surfaceKey: 'billing',
    operationKey: 'subscription',
    title: title|default(meta.title|default('Billing Subscription')),
    subtitle: meta.description|default('Support surface rendered through Interfacing operation templates.'),
    actions: data.actions|default([]),
    data: data,
    meta: meta,
    view: view|default({}),
    component: component|default(view.component|default('Billing'))
  } only %}
  <section class="interfacing-support-grid" data-ui-zone="support-surface-grid" data-surface="billing" data-operation="subscription">
    <article data-ui-zone="support-primary">
      {% include '@Interfacing/partial/billing_account_panel.html.twig' with {data: data, meta: meta, title: meta.primaryTitle|default('Billing Subscription')} only %}
    </article>
    <article data-ui-zone="support-secondary">
      {% include '@Interfacing/partial/usage_meter_panel.html.twig' with {data: data, meta: meta, title: meta.secondaryTitle|default('Context')} only %}
    </article>
  </section>
{% endblock %}
