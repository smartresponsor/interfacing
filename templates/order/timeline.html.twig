{% extends '@Interfacing/order/base.html.twig' %}
{% block title %}{{ title|default('Order timeline') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% include '@Interfacing/partial/view_header.html.twig' with {viewKey:'order', operationKey:'timeline', title:title|default('Order timeline'), subtitle:meta.description|default('Chronological view of order lifecycle events.'), actions:data.actions|default([]), data:data, meta:meta|default({}), view:view|default({}), component:component|default('Ordering')} only %}
  {% include '@Interfacing/partial/status_timeline.html.twig' with {events:data.timeline|default(data.events|default([])), title:'Lifecycle events'} only %}
{% endblock %}
