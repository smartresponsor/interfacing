{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{% extends '@Interfacing/product/base.html.twig' %}
{% block title %}{{ title|default('Product reviews') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/surface_header.html.twig' with {surfaceKey: 'product', operationKey: 'review', title: title|default(meta.title|default('Product reviews')), subtitle: meta.description|default('Product reviews surface for product commerce workflows.'), actions: data.actions|default([]), data: data, meta: meta, view: view|default({}), component: component|default(view.component|default('Commercializing'))} only %}
  {% include '@Interfacing/partial/record_grid.html.twig' with {title: 'Product reviews', records: data.reviews|default(data.items|default(data.records|default([]))), emptyLabel: 'No product reviews were supplied by the producer payload.', data: data} only %}
{% endblock %}
