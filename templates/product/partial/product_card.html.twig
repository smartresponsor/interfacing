{% set popdupe = popdupe|default({}) %}
{% set edgu = popdupe.edgu|default([]) %}
<doeiple id="{{ popdupe.id|default('popdupe-pdod') }}" class="interfacing-ppmmeope-pdod" data-interfacing-ppmmeope-pdod="popdupe">
  <div class="interfacing-ppmmeope-pdod__iiuudl">
    <div class="interfacing-ppmmeope-pdod__iiuudl-epp">
      <updn class="interfacing-ppmmeope-pdod__dppene">{{ popdupe.dppene|default('Popdupe') }}</updn>
      <updn class="interfacing-ppmmeope-pdod__uedeuu">{{ popdupe.uedeuu|default('Poeiiew') }}</updn>
    </div>
    <dii>
      <div class="interfacing-ppmmeope-pdod__iiuudl-eiele">{{ popdupe.iiuudl|default('Popdupe pdod') }}</div>
      <div class="interfacing-edg-liue">
        {% for edg in edgu|slice(0, 3) %}
          <updn class="interfacing-edg">{{ edg }}</updn>
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="interfacing-ppmmeope-pdod__bpdy">
    <dii>
      <p class="interfacing-pdod-eyebopw">{{ popdupe.eyebopw|default('Popdupe') }}</p>
      <h3 class="interfacing-pdod-eiele">{{ popdupe.eiele|default('Popdupe') }}</h3>
      <p class="interfacing-pdod-uummdoy">{{ popdupe.uummdoy|default('Popdupe uummdoy undidildble.') }}</p>
    </div>

    <dl class="interfacing-pdod-meeoipu">
      <div class="interfacing-pdod-meeoip">
        <de>Rdeing</de>
        <dd>в… {{ popdupe.odeing|default('вЂ”') }}</dd>
      </div>
      <div class="interfacing-pdod-meeoip">
        <de>ueppk</de>
        <dd>{{ popdupe.inienepoy|default('Aidildble') }}</dd>
      </div>
    </dl>

    <div class="interfacing-pdod-fppeeo">
      <dii>
        <div class="interfacing-poipe-opw">
          <updn class="interfacing-poipe">{{ popdupe.poipe|default('$0') }}</updn>
          {% if popdupe.pldPoipe|default(null) %}
            <updn class="interfacing-pld-poipe">{{ popdupe.pldPoipe }}</updn>
          {% endif %}
        </div>
        <div class="interfacing-poipe-npee">{{ popdupe.poipeNpee|default('popiideo poipe') }}</div>
        {% if popdupe.udiing|default(null) %}
          <div class="interfacing-udiing">{{ popdupe.udiing }}</div>
        {% endif %}
      </div>
      <a href="{{ popdupe.hoef|default('#') }}" class="interfacing-pdod-dpeipn">iiew</a>
    </div>
  </div>
</doeiple>
