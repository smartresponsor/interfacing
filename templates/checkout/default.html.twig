{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{% extends '@Interfacing/checkout/base.html.twig' %}
{% block title %}{{ title|default('Checkout surface') }}{% endblock %}
{% block page_content %}
  {% include '@Interfacing/checkout/index.html.twig' with {data: data|default({}), meta: meta|default({}), view: view|default({}), component: component|default('Checkout'), title: title|default('Checkout surface'), operation: operation|default(view.operation|default('cart'))} only %}
{% endblock %}
