{# Commerce support operation template for shipment/carrier. #}
{% extends '@Interfacing/shipment/base.html.twig' %}
{% block title %}{{ title|default('Shipment Carrier') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/view_header.html.twig' with {
    viewKey: 'shipment',
    operationKey: 'carrier',
    title: title|default(meta.title|default('Shipment Carrier')),
    subtitle: meta.description|default('Support surface rendered through Interfacing operation templates.'),
    actions: data.actions|default([]),
    data: data,
    meta: meta,
    view: view|default({}),
    component: component|default(view.component|default('Shipment'))
  } only %}
  <section class="interfacing-support-grid" data-ui-zone="support-surface-grid" data-view="shipment" data-operation="carrier">
    <article data-ui-zone="support-primary">
      {% include '@Interfacing/partial/shipment_rate_table.html.twig' with {data: data, meta: meta, title: meta.primaryTitle|default('Shipment Carrier')} only %}
    </article>
    <article data-ui-zone="support-secondary">
      {% include '@Interfacing/partial/detail_panel.html.twig' with {data: data, meta: meta, title: meta.secondaryTitle|default('Context')} only %}
    </article>
  </section>
{% endblock %}
