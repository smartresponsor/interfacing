{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{% extends 'base.html.twig' %}
{% block body %}
  <div style="display:flex;justify-content:space-between;align-items:center;gap:10px;margin-bottom:10px;">
    <div>
      <div style="font-weight:600;">{{ spec.title }}</div>
      <div style="opacity:.65;font-size:12px;margin-top:2px;">/interfacing/{{ spec.id }}</div>
    </div>
    <a href="/interfacing" style="text-decoration:none;opacity:.75;">Back</a>
  </div>

  {{ live_component(component, { context: context }) }}
{% endblock %}
