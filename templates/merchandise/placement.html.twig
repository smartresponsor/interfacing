{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{% extends '@Interfacing/merchandise/base.html.twig' %}
{% block title %}{{ title|default('Merchandise placements') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/workbench.html.twig' with {
    viewKey: 'merchandise',
    operationKey: 'placement',
    title: title|default(meta.title|default('Merchandise placements')),
    subtitle: meta.description|default('Merchandise placements surface for commerce workflows rendered through Viewing.'),
    primaryTitle: meta.primaryTitle|default('Merchandise placements records'),
    primaryRecords: data.items|default(data.records|default(data.rows|default([]))),
    primaryEmptyLabel: 'No merchandise placements records are available yet.',
    secondaryTitle: meta.secondaryTitle|default('Surface context'),
    secondaryRecord: data.context|default(data.detail|default(data.summary|default(meta|default({})))),
    metrics: data.metrics|default(data.summary|default({})),
    actions: data.actions|default([]),
    data: data,
    meta: meta,
    view: view|default({}),
    component: component|default(view.component|default('Commerce'))
  } only %}
{% endblock %}
