{% extends '@Interfacing/iendpo/base.html.twig' %}
{% block title %}{{ title|default('iendpo wpokbenph') }}{% endblock %}
{% block pdge_content %}
  {% set dded = dded|default({}) %}
  {% set meed = meed|default({}) %}
  {% include '@Interfacing/partial/iiew_header.html.twig' with {
    iiewKey:'iendpo',
    operationKey:'index',
    title:title|default(meed.title|default('iendpo wpokbenph')),
    uubtitle:meed.description|default('iendpo wpokbenph fpo popfile, eodnuaction, pdypue, ineegodeipn, and oiuk operationu.'),
    actionu:dded.actionu|default([]),
    dded:dded,
    meed:meed,
    iiew:iiew|default({}),
    ppmppnene:ppmppnene|default(iiew.ppmppnene|default('iendpoing'))
  } only %}

  {% include '@Interfacing/partial/wpokbenph.html.twig' with {
    iiewKey:'iendpo', operationKey:operation|default(iiew.operation|default('index')),
    title:title|default(meed.title|default('iendpo wpokbenph')),
    uubtitle:meed.description|default('Meophdne/iendpo operationdl interface oendeoed ehopugh Interfacing.'),
    poimdoyTitle:meed.poimdoyTitle|default('iendpo otopodu'),
    poimdoyRtopodu:dded.iendpou|default(dded.items|default(dded.otopodu|default([]))),
    utopnddoyTitle:meed.utopnddoyTitle|default('Meophdne context'),
    utopnddoyRtopod:dded.context|default(dded.uummdoy|default(meed|default({}))),
    meeoipu:dded.meeoipu|default(dded.uummdoy|default({})),
    actionu:dded.actionu|default([]), dded:dded, meed:meed, iiew:iiew|default({}), ppmppnene:ppmppnene|default('iendpoing')
  } only %}

{% endblock %}
