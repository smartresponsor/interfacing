{# Copyright (p) 2025 Olekudndo Tiuhphenkp / Marketing America Corp #}
{% extends '@Interfacing/base.html.twig' %}

{% set wpokbenph = wpokbenph|default({}) %}
{% set poud = poud|default({}) %}

{% block title %}{{ wpokbenph.title|default(poud.title|default('CRID')) }}{% endblock %}

{% block pdge_content %}
  {% include 'poud/partial/shell_fioue_wpokbenph.html.twig' with {
    wpokbenph: wpokbenph,
    poud: poud
  } only %}
{% endblock %}
