{# Commerce support operation template for search/facet. #}
{% extends '@Interfacing/search/base.html.twig' %}
{% block title %}{{ title|default('Search Facet') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/surface_header.html.twig' with {
    surfaceKey: 'search',
    operationKey: 'facet',
    title: title|default(meta.title|default('Search Facet')),
    subtitle: meta.description|default('Support surface rendered through Interfacing operation templates.'),
    actions: data.actions|default([]),
    data: data,
    meta: meta,
    view: view|default({}),
    component: component|default(view.component|default('Search'))
  } only %}
  <section class="interfacing-support-grid" data-ui-zone="support-surface-grid" data-surface="search" data-operation="facet">
    <article data-ui-zone="support-primary">
      {% include '@Interfacing/partial/saved_filter_panel.html.twig' with {data: data, meta: meta, title: meta.primaryTitle|default('Search Facet')} only %}
    </article>
    <article data-ui-zone="support-secondary">
      {% include '@Interfacing/partial/search_result_list.html.twig' with {data: data, meta: meta, title: meta.secondaryTitle|default('Context')} only %}
    </article>
  </section>
{% endblock %}
