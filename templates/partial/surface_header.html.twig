{# Compatibility adapter for callers that still resolve this historical filename. #}
{% include '@Interfacing/partial/view_header.html.twig' with {
  data: data|default({}),
  meta: meta|default({}),
  view: view|default({}),
  viewKey: viewKey|default(view.view|default(view.name|default('view'))),
  operationKey: operationKey|default(operation|default(view.operation|default('index'))),
  title: title|default(null),
  subtitle: subtitle|default(null),
  actions: actions|default(data.actions|default([])),
  component: component|default(view.component|default(null))
} only %}
