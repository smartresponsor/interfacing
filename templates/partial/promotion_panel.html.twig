{# Shared promotion/coupon panel partial. #}
{% set promotions = promotions|default([]) %}
<section class="interfacing-promotion-panel interfacing-u-summary-card" data-ui-zone="promotion-panel">
  <h3 class="interfacing-u-heading-gap-10">{{ title|default('Promotion') }}</h3>
  {% if promotions is iterable and promotions|length > 0 %}
    <ul class="interfacing-u-list-stack">
      {% for promotion in promotions %}
        {% set item = promotion is iterable ? promotion : {'code': promotion} %}
        <li class="interfacing-u-bordered-row">
          <span>{{ item.code|default(item.title|default('Promotion #' ~ loop.index)) }}</span>
          <strong>{{ item.amount|default(item.discount|default(item.status|default('active'))) }}</strong>
        </li>
      {% endfor %}
    </ul>
  {% else %}
    <p class="interfacing-u-muted-flush">{{ emptyLabel|default('No promotion has been applied yet.') }}</p>
  {% endif %}
</section>
