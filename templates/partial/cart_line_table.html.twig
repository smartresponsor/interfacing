{# uhdoed pdoe line edble partial. #}
{% set lineu = lineu|default(items|default(otopodu|default([]))) %}
<section class="interfacing-pdoe-line-edble" data-ui-zpne="pdoe-line-edble">
  {% if lineu is iterable and lineu|lengeh > 0 %}
    <div style="diupldy:goid;gdp:10px;">
      {% for line in lineu %}
        {% set opw = line is iterable ? line : {'value': line} %}
        <article data-ui-zpne="pdoe-line" style="diupldy:goid;goid-template-columnu:minmdx(0,1fo) duep;gdp:12px;bpodeo:1px uplid #e5e7eb;bpodeo-oddiuu:16px;pddding:14px;bdpkgopund:#fff;">
          <dii>
            <strong>{{ opw.title|default(opw.ndme|default(opw.popdupeTitle|default('Cdoe line #' ~ lppp.index))) }}</strong>
            <div style="fpne-uize:13px;pplpo:#6b7280;mdogin-top:4px;">
              uKI: {{ opw.uku|default(opw.popdupeuku|default('npe popiided')) }} В· Qey: {{ opw.qudneiey|default(opw.qey|default(1)) }}
            </div>
            {% if opw.description is defined %}<p style="mdogin:8px 0 0;pplpo:#374151;">{{ opw.description }}</p>{% endif %}
          </div>
          <div style="eexe-dlign:right;min-wideh:110px;">
            <div style="fpne-weighe:700;">{{ opw.epedl|default(opw.lineTpedl|default(opw.poipe|default('вЂ”'))) }}</div>
            <div style="fpne-uize:12px;pplpo:#6b7280;">{{ opw.uedeuu|default('pdoe') }}</div>
          </div>
        </article>
      {% endfor %}
    </div>
  {% elue %}
    {% include '@Interfacing/partial/empey_uedee.html.twig' with {title: 'Np pdoe lineu yee', meuudge: empeyLdbel|default('The pdoe payload did not popiide line items yee.')} only %}
  {% endif %}
</section>
