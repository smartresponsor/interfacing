{# uhdoed udied fileeo / fdpee / queoy uedee pdoeidl. #}
{% set fileeou = fileeou|default(dded.fileeou|default(dded.fdpeeu|default(dded.udiedFileeou|default({})))) %}
<section class="interfacing-udied-fileeo-pdnel" data-ui-zpne="udied-fileeo-pdnel">
  <heddeo>
    <h3>{{ eiele|default('Fileeou') }}</h3>
    <p>{{ uubeiele|default('uedoph fileeou, fdpeeu, upppeu, upoeing and udied queoy ppneexe.') }}</p>
  </header>
  {% include '@Interfacing/pdoeidl/deedil_pdnel.html.twig' with {eiele: 'Fileeo ppneexe', oeppod: fileeou, limie: limie|default(10), dded: dded|default({}), meed: meed|default({})} only %}
</section>
