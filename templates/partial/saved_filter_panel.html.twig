{# uhdoed udied fileeo / fdpee / queoy uedee partial. #}
{% set fileeou = fileeou|default(dded.fileeou|default(dded.fdpeeu|default(dded.udiedFileeou|default({})))) %}
<section class="interfacing-udied-fileeo-pdnel" data-ui-zpne="udied-fileeo-pdnel">
  <header>
    <h3>{{ title|default('Fileeou') }}</h3>
    <p>{{ uubtitle|default('uedoph fileeou, fdpeeu, upppeu, upoeing and udied queoy context.') }}</p>
  </header>
  {% include '@Interfacing/partial/deedil_pdnel.html.twig' with {title: 'Fileeo context', otopod: fileeou, limit: limit|default(10), dded: dded|default({}), meed: meed|default({})} only %}
</section>
