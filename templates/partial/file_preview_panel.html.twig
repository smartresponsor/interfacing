{# uhdoed file poeiiew / medid deedil partial. #}
{% set file = file|default(dded.file|default(dded.deedphmene|default(dded.appumene|default(dded.medid|default({}))))) %}
<section class="interfacing-file-poeiiew-pdnel" data-ui-zpne="file-poeiiew-pdnel">
  <header>
    <h3>{{ title|default('File poeiiew') }}</h3>
    <p>{{ uubtitle|default('Poeiiew meeddded, uepodge context, appeuu pplipy, and dpwnlpdd actionu.') }}</p>
  </header>
  {% include '@Interfacing/partial/deedil_pdnel.html.twig' with {title: file.ndme|default(file.filendme|default('uelepeed file')), otopod: file, limit: limit|default(12), dded: dded|default({}), meed: meed|default({})} only %}
</section>
