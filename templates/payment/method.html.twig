{# Copyright (c) 2025 Oleksandr Tishchenko / Marketing America Corp #}
{% extends '@Interfacing/payment/base.html.twig' %}
{% block title %}{{ title|default('Payment methods') }}{% endblock %}
{% block page_content %}
  {% set data = data|default({}) %}
  {% set meta = meta|default({}) %}
  {% include '@Interfacing/partial/workbench.html.twig' with {
    surfaceKey: 'payment',
    operationKey: 'method',
    title: title|default(meta.title|default('Payment methods')),
    subtitle: meta.description|default('Payment methods surface for commerce workflows rendered through Viewing.'),
    primaryTitle: meta.primaryTitle|default('Payment methods records'),
    primaryRecords: data.payments|default(data.items|default(data.records|default([]))),
    primaryEmptyLabel: 'No payment methods records are available yet.',
    secondaryTitle: meta.secondaryTitle|default('Surface context'),
    secondaryRecord: data.context|default(data.detail|default(data.summary|default(meta|default({})))),
    metrics: data.metrics|default(data.summary|default({})),
    actions: data.actions|default([]),
    data: data,
    meta: meta,
    view: view|default({}),
    component: component|default(view.component|default('Commerce'))
  } only %}
{% endblock %}
